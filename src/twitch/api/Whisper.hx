package twitch.api;

import haxe.http.HttpMethod;
import twitch.api.APIEndpoint;

/** Endpoints pertaining to sending private messages (whispers) to users. **/
@:build(twitch.api.APIBuilder.build("whisper"))
class Whisper {
}

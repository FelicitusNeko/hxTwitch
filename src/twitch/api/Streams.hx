package twitch.api;

import haxe.http.HttpMethod;
import twitch.api.APIEndpoint;

/** Endpoints pertaining to streams and stream markers. **/
@:build(twitch.api.APIBuilder.build("streams"))
class Streams {
}

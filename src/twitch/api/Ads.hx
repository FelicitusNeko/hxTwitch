package twitch.api;

import haxe.http.HttpMethod;
import twitch.api.APIEndpoint;

/** Endpoints pertaining to running advertisements during a stream. **/
@:build(twitch.api.APIBuilder.build("ads"))
class Ads {
}

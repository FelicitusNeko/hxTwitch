package twitch.api;

import haxe.http.HttpMethod;
import twitch.api.APIEndpoint;

/** Endpoints pertaining to moderating channels, including AutoMod, blocked terms, and user management. **/
@:build(twitch.api.APIBuilder.build("moderation"))
class Moderation {
}

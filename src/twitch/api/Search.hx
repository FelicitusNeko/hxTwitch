package twitch.api;

import haxe.http.HttpMethod;
import twitch.api.APIEndpoint;

/** Endpoints pertaining to searching for channels and categories. **/
@:build(twitch.api.APIBuilder.build("search"))
class Search {
}
